<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.fitnessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WorkoutsPage" />
        <activity android:name=".ProgressPage" />
        <activity android:name=".GoalsPage" />
        <activity
            android:name=".ExersicePage"
            android:label="@string/title_activity_exersice_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ExercisePage"
            android:label="@string/title_activity_exercise_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ExercisePageEditor"></activity>
    </application>

</manifest>